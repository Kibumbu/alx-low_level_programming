#include <stdio.h>

/**
 *  main -> prints all single digit numbers
 *  All your code should be in the main function
 *  Return: always 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d\n", n);
	}

	return (0);
}
