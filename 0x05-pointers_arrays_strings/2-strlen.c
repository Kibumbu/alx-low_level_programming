#include "main.h"
#include <string.h>

/**
 *_strlen -> function to get the length of a string
 *@s: string printer to passed to this function
 *Return: returns length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
